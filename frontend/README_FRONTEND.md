
# Frontend (React) - Notas

- Porta servida via nginx no container: 80 (mapear para 3000 no host pelo docker-compose).
- Configurar variável REACT_APP_API_URL se a API não rodar em http://localhost:4000

