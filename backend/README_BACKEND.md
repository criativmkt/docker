
# Backend (NestJS) - Notas

- Porta exposta: 4000
- Endpoints:
  - GET /expenses
  - POST /expenses
  - DELETE /expenses/:id

- A aplicação usa TypeORM com synchronize=true para criar automaticamente a tabela no SQL Server (apenas para desenvolvimento).

